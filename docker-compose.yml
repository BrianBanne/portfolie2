version: "3"
services:
  client:
    build:
      context: client
      dockerfile: Dockerfile
    volumes:
      - "./client:/app"
    ports:
      - "8082:3000"
    depends_on:
      - server
  server:
    build:
      context: server
      dockerfile: Dockerfile
    volumes:
      - "./server:/app"
    ports:
      - "8081:8080"
    depends_on:
      - db
  db:
    image: mongo:latest
    container_name: mongodb
    volumes:
      - mongodb_data:/data/db
    ports:
      - 27017:27017
volumes:
  mongodb_data:
